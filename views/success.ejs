<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Resultado</title>
</head>
<body>
  <h1>Resultados de Formulario signo del Zodiaco</h1>

  <% users.forEach(user => { %>
    <h2>User: <%= user.name %></h2>

    <ul>
      <% user.answers.forEach(answer => { %>
        <li><%= answer.question %>: <%= answer.answer %></li>
      <% }) %>
    </ul>
  <% }) %>

  <p>Guardar Resultados:</p>
  <ul>
    <li><a href="/download/excel">Guardar Excel</a></li>
    <li><a href="/download/csv">Guardar CSV</a></li>
  </ul>
</body>
</html>
